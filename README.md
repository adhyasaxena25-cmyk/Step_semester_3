# Step_semester_3
Step assignments, class work and tests.


## [13-09-2026] - Week 6: Access Modifiers, Encapsulation & Object Modeling

### Summary
- Learned and applied Java access modifiers (`private`, `default`, `protected`, `public`), encapsulation practices, static members, and object reference handling.
- Standardized package structure across all files to `main.java.week_6.class_work`.

### Completed Classwork Problems
1. **Student Placement Record Management (`PlacementRecord.java`)**: Designed an object-oriented replacement for parallel arrays using a custom class and array of objects.
2. **Hostel Mess Wallet Management (`MessWallet.java`)**: Implemented strict encapsulation with a `private` balance field, getter-only read access, and validated mutation methods (`topUp`, `deduct`).
3. **Course Credit Management (`Course.java`)**: Demonstrated constructor chaining using `this(...)` to handle theory-only and lab-integrated courses.
4. **Library ID Card Management (`IdCard.java`)**: Analyzed object reference behavior vs. separate instance creation using the `==` operator.
5. **Student and College Information Management (`Student.java`)**: Utilized static fields (`collegeName`, `studentCount`) and a static utility method to manage shared context across instances.

### Repository Layout Verification
- Branch: `feature/session_6` (Merged into `develop`)
- Path: `src/main/java/week_6/class_work/`

---

## Date: 12-09-2026
**Today's Work:**
- Completed and committed all 5 Week 5 Category C Live-Coding (Classwork) problems under `src/main/java/week_5/class_work/`.
- Completed and committed all 5 Week 5 Category C Assignment problems under `src/main/java/week_5/assignment_problems/`.

**Next Session Plan:**
- Merge `feature/session_5` into `develop` via Pull Request and prepare for Week 6.

**Issues Faced:**
- Managed arrays passed by reference, single-pass min/max tracking, 2D jagged grid processing, and Comparable interface implementations.

---

## Date: 12-09-2026
**Today's Work:**
- Completed and committed all 5 Week 4 Category C Live-Coding (Classwork) problems under `src/main/java/week_4/class_work/`.
- Completed and committed all 5 Week 4 Category C Assignment problems under `src/main/java/week_4/assignment_problems/`.

**Next Session Plan:**
- Merge `feature/session_4` into `develop` via Pull Request and begin Week 5 tasks.

**Issues Faced:**
- Solved array problems focusing on $O(n)$ time complexity, two-pointer approach, and Kadane's algorithm without using division.

---

## Date: 12-09-2026
**Today's Work:**
- Completed and committed all 5 Week 2 Live-Coding (Classwork) problems under `src/main/java/week_2/class_work/`.
- Completed and committed all 5 Week 2 Assignment problems under `src/main/java/week_2/assignment_problems/`.

**Next Session Plan:**
- Merge `feature/session_2` into `develop` via Pull Request and prepare for Week 3.

**Issues Faced:**
- Properly handled custom string parsing, normalization, and stop-word filtering without regex.

---

## Date: 12-09-2026
**Today's Work:**
- Completed and committed all 5 Week 1 Live-Coding (Classwork) problems under `src/main/java/week_1/class_work/`.
- Completed and committed all 5 Week 1 Assignment problems under `src/main/java/week_1/assignment_problems/`.

**Next Session Plan:**
- Branch off `develop` to create `feature/session_2` and start Week 2 topics.

**Issues Faced:**
- Fixed package statement alignment and cleaned compiled `.class` files from Git tracking.